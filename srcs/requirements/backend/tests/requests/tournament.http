# Using VScode extension REST Client (install it and click on 'Send Request' that appears above each request, separate requests with '###')

# Change 'http://localhost:3000/' to 'https://padaria.42.pt/api/' to test through Frontend container

### TOURNAMENT

### Get a specific tournament's participants' data
GET http://localhost:3000/tournaments/<id> HTTP/1.1

### Get data (position and type) on the last three tournaments from a specific user
GET http://localhost:3000/tournaments/user/<id> HTTP/1.1
