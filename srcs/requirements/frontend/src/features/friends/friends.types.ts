export interface friend {
  createdAt: string;
  friendId: string;
  id: string;
  status: string;
  updatedAt: string;
  userId: string;
}

export interface friendData {
  id: string;
  username: string;
  score: number;
  rank: number;
  avatarUrl: string;
  onlineState: string;
}
