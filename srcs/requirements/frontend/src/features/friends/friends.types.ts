export interface friend {
  createdAt: string;
  friendId: string;
  id: string;
  status: string;
  updatedAt: string;
  userId: string;
}

export interface friendData {
  id: string;
  name: string;
  points: number;
  rank: number;
  avatar: string;
  status: string;
}
