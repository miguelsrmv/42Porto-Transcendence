export interface friend {
  createdAt: string;
  friendId: string;
  id: string;
  status: string;
  updatedAt: string;
  userId: string;
}

export interface friendData {
  id: string;
  username: string;
  points: number;
  rank: number;
  avatarUrl: string;
  status: string;
}
