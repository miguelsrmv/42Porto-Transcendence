/**
 * @file backgroundData.types.ts
 * @brief Defines the interface for background data in the game.
 *
 * This file contains the interface definition for background data, which includes
 * properties for the background's name and the path to its image.
 */

/**
 * @brief Interface for background data.
 *
 * This interface defines the structure for background data objects, including the background's name
 * and the path to its image file.
 */
export interface backgroundData {
  name: string;
  imagePath: string;
}
